apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: shopregister
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80 # frontend contaner ngix ip
      targetPort: 80 #pode port
      nodePort: 30080
  type: NodePort
